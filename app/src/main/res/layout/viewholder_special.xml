<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@drawable/brown_bg"
    android:layout_margin="8dp"
    android:layout_height="wrap_content">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/picMain"
        app:shapeAppearanceOverlay="@style/CircleImageView"
        android:layout_width="145dp"
        android:scaleType="centerCrop"
        android:layout_height="95dp"
        android:layout_margin="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@tools:sample/backgrounds/scenic" />

    <TextView
        android:id="@+id/titleTxtSpecial"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginStart="16dp"
        android:text="title"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/white"
        app:layout_constraintStart_toEndOf="@+id/picMain"
        app:layout_constraintTop_toTopOf="@+id/picMain" />

    <RatingBar
        android:id="@+id/ratingBar"
        style="@style/Widget.AppCompat.RatingBar.Small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:numStars="5"
        android:progressTint="@color/orange"
        android:rating="4"
        app:layout_constraintStart_toStartOf="@+id/titleTxtSpecial"
        app:layout_constraintTop_toBottomOf="@+id/titleTxtSpecial" />

    <TextView
        android:id="@+id/priceTxtSpecial"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:textStyle="bold"
        android:textColor="@color/white"
        android:text="₹-"
        app:layout_constraintBottom_toBottomOf="@+id/picMain"
        app:layout_constraintStart_toStartOf="@+id/ratingBar" />
</androidx.constraintlayout.widget.ConstraintLayout>